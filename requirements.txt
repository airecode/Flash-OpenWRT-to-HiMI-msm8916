# Python dependencies for bkerler/edl plus the helpers in scripts/.
# Install into a venv:  python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
wheel
pyusb>=1.3.1
pyserial>=3.5
docopt>=0.6.2
pycryptodome>=3.23.0
pycryptodomex
lxml>=6.0.2
colorama
capstone
keystone-engine
qrcode
requests>=2.34.2
passlib
Exscript>=2.6.32
paramiko>=4.0.0,<5

# Used by scripts/edl-drain.py and the verification helpers
# (Pillow/numpy only needed if you redact your own board photos)
